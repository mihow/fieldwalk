import SwiftUI

struct ContentView: View {
    var body: some View {
        NavigationStack {
            Text("FieldWalk")
                .font(.largeTitle)
                .navigationTitle("Surveys")
        }
    }
}
